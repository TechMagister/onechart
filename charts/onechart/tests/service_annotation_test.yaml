suite: test deployment
templates:
  - service.yaml
tests:
  - it: Should set annotations if it is defined
    set:
      serviceAnnotations:
        test: example
    asserts:
      - equal:
          path: metadata.annotations
          value:
            test: example